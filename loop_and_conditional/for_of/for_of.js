// for...of – duyệt mảng hoặc chuỗi
// ✅ Lý thuyết:

// Dùng để lặp qua các giá trị của một mảng, chuỗi hoặc đối tượng iterable (như Set, Map).

const colors = ["red", "green", "blue"];

for (const color of colors) {
    console.log(color);
}